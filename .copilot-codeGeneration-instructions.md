# Instructions for LLM Code Generation in Hydrological Time Series Analysis

## Project Overview

This project focuses on hydrological modeling using deep learning techniques to analyze rainfall-runoff dynamics. It leverages time series data from various catchments to build models that can transfer knowledge between regions (e.g., from data-rich European basins to data-sparse Central Asian basins). The core objective is to improve hydrological predictions in both gauged and ungauged basins through various deep learning architectures including LSTMs, EA-LSTMs, and TSMixer models.

## Tech Stack

- **Python**: Core language
- **PyTorch & PyTorch Lightning**: Deep learning framework
- **pandas & NumPy**: Data manipulation
- **scikit-learn**: Preprocessing pipelines and metrics
- **dtwcluster/dtaidistance**: Time series clustering with Dynamic Time Warping
- **Matplotlib & Seaborn**: Visualization

## Code Generation Guidelines

### Structure and Style

1. Use clear, descriptive variable and function names that reflect hydrological domain concepts
2. Structure code in logical modules (data loading, preprocessing, modeling, evaluation)
3. Follow PEP 8 conventions with consistent spacing and line length
4. Include appropriate imports at the top of files

### Type Hints

1. Use comprehensive type hints for all function parameters and return values
2. Leverage Union, Optional, List, Dict, Tuple, and other typing constructs
3. Create descriptive custom types using TypedDict or dataclasses where appropriate
4. Include typing for collections (e.g., `List[float]` instead of just `List`)

```python
def load_catchment_data(
    gauge_ids: List[str], 
    start_date: Optional[str] = None
) -> Dict[str, pd.DataFrame]:
    """Load catchment data for specified gauge IDs."""
    # Implementation here
```

### Docstrings

1. Include concise but informative docstrings for all functions and classes
2. Follow Google-style docstring format with Args, Returns, and Raises sections when appropriate
3. Describe parameters, return values, and exceptions clearly but concisely
4. Include examples for complex functions

```python
def apply_log_transform(
    df: pd.DataFrame,
    columns: List[str],
    epsilon: float = 1e-8
) -> pd.DataFrame:
    """
    Apply log1p transform to specified columns with epsilon handling.
    
    Args:
        df: Input dataframe containing time series data
        columns: Column names to transform
        epsilon: Small constant to add before transform to handle zeros
        
    Returns:
        DataFrame with transformed values
        
    Raises:
        ValueError: If any column not found in dataframe
    """
    # Implementation here
```
